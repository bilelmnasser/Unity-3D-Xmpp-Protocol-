
using System;

namespace Xmpp.protocol.extensions.bytestreams
{
    /// <summary>
    /// The Mode for the bytestream socket layer (tcp or udp)
    /// </summary>
    public enum Mode
    {
        NONE = -1,
        tcp,
        udp
    }
}
